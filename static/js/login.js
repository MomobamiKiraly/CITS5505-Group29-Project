document.addEventListener('DOMContentLoaded', () => {
  // Log when the login button is clicked
  document.querySelector('.btn').addEventListener('click', () => {
    console.log("Login submitted.");
  });
});